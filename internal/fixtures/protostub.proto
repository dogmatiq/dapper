syntax="proto3";
package dogmatiq.dapper.fixtures;

option go_package = "github.com/dogmatiq/dapper/internal/fixtures";

message Message {
  string first_field = 1;
  Enum enum_field = 2;
  Nested nested = 3;
}

message Nested {
  string nested_first_field = 1;
  bool nested_second_field = 2;
}

enum Enum {
  UNKNOWN = 0;
  FOO = 1;
  BAR = 2;
}
